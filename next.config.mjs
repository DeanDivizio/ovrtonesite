/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
          domains: ['newapi.ovrtonemedia.com', 'ovrtonemedia.com', 'localhost'],
        },
};

export default nextConfig;
